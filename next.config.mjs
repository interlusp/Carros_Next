/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','img.olx.com.br','image.webmotors.com.br','image.webmotors.com.br'],
    },
}

export default nextConfig
